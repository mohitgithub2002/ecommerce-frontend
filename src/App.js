import React from 'react';
import ReactDOM from 'react-dom';
import PictureGallery from './productpage';
const App = () => {
  ReactDOM.render(
    <PictureGallery />,
    document.getElementById('root')
  );
};

export default App;
